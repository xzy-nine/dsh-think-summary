# dependencies / caches
node_modules/
.npm-cache/

# local memory notes (开发记忆，不纳入版本库)
memory/

# build output (npm pack 在发布时构建)
lib/
*.tsbuildinfo

# library: lockfile 不纳入版本库
package-lock.json
pnpm-lock.yaml

# logs
*.log

# temp (commit message drafts etc.)
*.tmp
COMMIT_MSG*
